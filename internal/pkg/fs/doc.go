// Package fs provides methods for interacting with the filesystem,
// as an abstraction layer.

// Its side goal is also to help removing the files and
// directories created while performing a conversion.
package fs
